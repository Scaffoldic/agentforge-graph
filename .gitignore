.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# CKG index artifacts (derivable; never committed — ADR-0006)
.ckg/

# Local-only framework learnings (issues, hacks, upgrade notes)
docs/framework/

# Local-only roadmap + release/verification notes (not published)
docs/roadmap/

# Local-only prior-art survey (not published)
docs/open-source-ckg-research.md

# Local-only feature-analysis staging (proposals under review, pre-tracker)
docs/feature-analysis/

# AI-assistant per-session working state (local-only, not published)
.claude/state/

# macOS
.DS_Store

# Keep .agentforge-state checked in — it tracks framework managed
# files for upgrade.

# build artifacts (uv build / hatchling)
/dist/
